# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/worktrees/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.claude/scheduled_tasks.lock

# Tauri sidecars — fetched at build time by scripts/fetch-ffmpeg.sh.
# Self-contained builds (~50–80 MB each) are too large to commit.
# The directory itself is kept alive by src-tauri/binaries/README.md.
src-tauri/binaries/ffmpeg-*
src-tauri/binaries/ffprobe-*
